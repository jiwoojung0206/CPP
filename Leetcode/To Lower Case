class Solution {
public:
    
    
    string toLowerCase(string str) {
        
        for (int i = 0; i < str.length(); ++i)
            str.at(i) = tolower(str.at(i));
        
    return str;
    }
};
